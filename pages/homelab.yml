- name: Homelab
  columns:
    - size: small
      widgets:
        $include: ../widgets/proxmox.yml

     - size: full
       widgets:
         $include: ../widgets/monitors.yml

    # - size: small
    #   widgets:
    #     $include: ../widgets/pihole.yml
    #     $include: ../widgets/bookmarks.yml
