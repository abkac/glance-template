- name: Homelab
  columns:
    - size: small
      widgets:
        $include: ../widgets/proxmox-stats.yml
        $include: ../widgets/proxmox-nodes.yml

    - size: full
      widgets:
        $include: ../widgets/monitors.yml

    - size: small
      widgets:
        $include: ../widgets/pihole.yml
